[tool.poetry]
name = "adihtml"
version = "0.1.0"
description = "A little library to build websites in Python, simply."
authors = ["Adithya Yerramsetty <adithyay@hotmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
