# Organization
The way this has been organized has changed a couple times.

Initially, it was a set of Python generator files disperesed throughout the serve directory,
called manually to generate the HTML files we cared about. I don't like that.

For my old site, NextJS was the site generator of choice.
